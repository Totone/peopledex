// import Link from "next/link";
// import { Button } from "@/ui/primitives/button";

// type NavButtonIcon = "plus" | "search";

// type NavButtonProps = {
//   icon: NavButtonIcon;
//   variant: (typeof Button)["variant"];
// };

// export const NavButton: React.FC<NavButtonProps> = ({ icon }) => {};
